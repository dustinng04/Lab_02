<Window x:Class="YourNamespace.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Login" FontSize="20" HorizontalAlignment="Center" Margin="0,20,0,10" />

        <Label Grid.Row="1" Content="Username:" HorizontalAlignment="Left" Margin="50,0,0,0" />
        <TextBox Grid.Row="1" x:Name="UsernameTextBox" HorizontalAlignment="Stretch" Margin="150,0,50,0" />

        <Label Grid.Row="2" Content="Password:" HorizontalAlignment="Left" Margin="50,10,0,0" />
        <PasswordBox Grid.Row="2" x:Name="PasswordBox" HorizontalAlignment="Stretch" Margin="150,10,50,0" />

        <Button Grid.Row="3" Content="Login" HorizontalAlignment="Center" Margin="0,20,0,0" Width="100" Click="LoginButton_Click" />

        <TextBlock Grid.Row="4" x:Name="ErrorMessageText" Foreground="Red" HorizontalAlignment="Center" Margin="0,10,0,0" />
    </Grid>
</Window>